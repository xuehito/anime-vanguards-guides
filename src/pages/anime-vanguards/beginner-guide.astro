---
import { getEntry, render } from 'astro:content';
import GuideLayout from '../../layouts/GuideLayout.astro';

const entry = await getEntry('guides', 'anime-vanguards-beginner-guide');
if (!entry) throw new Error('Missing content: anime-vanguards-beginner-guide');
const { Content } = await render(entry);
const { title, description, updated, type } = entry.data;
---

<GuideLayout title={title} description={description} updated={updated} type={type}>
  <Content />
</GuideLayout>
