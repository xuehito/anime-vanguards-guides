---
import { getEntry, render } from 'astro:content';
import GuideLayout from '../../layouts/GuideLayout.astro';
import RerollCalculator from '../../components/RerollCalculator.astro';

const entry = await getEntry('guides', 'anime-vanguards-trait-reroll-odds');
if (!entry) throw new Error('Missing content: anime-vanguards-trait-reroll-odds');
const { Content } = await render(entry);
const { title, description, updated, type, patch } = entry.data;
---

<GuideLayout
  title={title}
  description={description}
  updated={updated}
  type={type}
  patch={patch}
>
  <RerollCalculator />
  <Content />
</GuideLayout>
