---
import { getEntry, render } from 'astro:content';
import GuideLayout from '../../layouts/GuideLayout.astro';
import SummonPityCalculator from '../../components/SummonPityCalculator.astro';

const entry = await getEntry('guides', 'anime-vanguards-summon-pity');
if (!entry) throw new Error('Missing content: anime-vanguards-summon-pity');
const { Content } = await render(entry);
const { title, description, updated, type, patch } = entry.data;
---

<GuideLayout
  title={title}
  description={description}
  updated={updated}
  type={type}
  patch={patch}
>
  <SummonPityCalculator />
  <Content />
</GuideLayout>
